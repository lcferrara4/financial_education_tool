#ifndef TAX_H
#define TAX_H

#include <iostream>
#include <string>
#include "Loan.h"
#include "Mortgage.h"
#include <vector>

using namespace std; 
class Tax{

    friend class Loan; 

    public: 
        Tax(Loan); 
        //~Tax(); 
        void getDeductions(Loan); 
        void calcItax(); 
    private:
        double income; 
        string state; 
        double stateRate; 
        double fedRate; 
        double deductions;
        vector<vector<string> > singleState; 
        vector<vector<string> > marriedState; 
        vector<vector<string> > singleFed; 
        vector<vector<string> > marriedFed; 
        string clean(string); 

};

#endif
